[build-system]
requires = ["setuptools>=75.0", "setuptools-scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "corecycler"
dynamic = ["version"]
description = "Per-core CPU stability tester and PBO Curve Optimizer tuner for AMD Ryzen on Linux"
readme = "README.md"
license = "GPL-3.0-or-later"
requires-python = ">=3.12"
authors = [{ name = "Daaboulex" }]
maintainers = [{ name = "Hleb Shauchenka" }]
dependencies = ["PySide6>=6.7"]

[project.scripts]
corecycler = "corecycler.main:main"

[project.optional-dependencies]
dev = ["pytest>=8.0", "ruff>=0.8", "hypothesis>=6.100"]

[tool.setuptools.packages.find]
where = ["src"]

# The release line this tree belongs to. Git checkouts derive the real version
# from tags (<next>.devN+g<sha>); this literal is what a tree without .git
# reports, and the flake appends the commit to it. Bump it when cutting a tag.
[tool.setuptools_scm]
fallback_version = "0.1.0"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
python_files = ["test_*.py"]
markers = [
  "slow: tests that take >5s (deselect with -m 'not slow')",
  "hardware: tests that verify hardware-specific detection behavior",
  "contract: Ring B live drift tests -- real binary/hardware, run outside the sandbox (pytest -m contract)",
]

[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM", "TCH", "TID251"]

# Regression-lock for the split-database bug class: under sudo these resolve
# to /root, silently forking app state. config.paths is the one allowed user.
[tool.ruff.lint.flake8-tidy-imports.banned-api]
"pathlib.Path.home".msg = "Path.home() is /root under sudo (split-database bug) — use corecycler.config.paths.user_home()"
"os.path.expanduser".msg = "expanduser('~') is /root under sudo — use corecycler.config.paths.user_home()"

[tool.ruff.lint.per-file-ignores]
"src/corecycler/config/paths.py" = ["TID251"]

[tool.coverage.run]
source = ["corecycler"]
branch = false
omit = ["*/main.py"]

[tool.coverage.report]
exclude_lines = [
  "pragma: no cover",
  "if TYPE_CHECKING:",
  "raise NotImplementedError",
  "@(abc\\.)?abstractmethod",
  "if __name__ == .__main__.:",
]
